<%= form_with(model: _user) do |form| %>
  <% if _user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(_user.errors.count, "error") %> prohibited this _user from being saved:</h2>

      <ul>
        <% _user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
